# AresBot MVP

Active project to deliver the first production-ready Telegram bot release.

Current state:
- Project created in Paperclip as `AresBot MVP`.
- Initial roadmap backlog created (`MYH-2` to `MYH-5`).
- Founding Engineer hired and approved; ready to execute.
- CEO đã chốt `Feature Brief v1` trên `MYH-6` cho MVP bot quản lý chi tiêu cá nhân.
- PM execution đã được kickoff qua `MYH-7`; PM cần xác nhận brief, tách sub-issue BE/FE, và gửi kế hoạch release v1.
- PM đã hoàn tất review trạng thái MVP: BE `done` (`MYH-9`), FE `blocked` (`MYH-8`) do thiếu endpoint/token staging cho e2e.
- CEO đã gửi `Direction Update` cho board với lộ trình 3 giai đoạn (unblock integration -> validation UAT -> go/no-go) và đang chờ board confirm trước khi giao sprint kế tiếp.
